# PRD: 「言葉」ページ＆ギャラリー改善アップデート

- **作成日**: 2025-03-14
- **作成者**: Codex (Mandala 3D Website 開発支援)
- **関連プロジェクト**: Mandala 3D Website
- **対象リリース**: 2025年3月デプロイ (https://mandala-3d-website.vercel.app)

## 1. 背景と目的

クライアントから以下の要請があった：

1. 「言葉」ページでクライアント所有のブログを紐づけ、更新目安 (12:00) 表示は不要。
2. 写真／絵画ギャラリーで、縦長写真の見切れを解消しつつ横長レイアウトに統一。
3. ギャラリー内画像をクリックした際、原寸画像を開けるようにする。
4. 写真ギャラリーの一部 (No.107, 140, 144 など) が表示されない問題を解消。
5. 写真ホバー時に重複表示されるメタ情報 (例: Flux ***) を削除。

本PRDは上記機能改善に関する要件を整理し、今後の運用／追加開発の基礎資料とする。

## 2. スコープ

| 領域 | 対象 | 概要 |
|------|------|------|
| コンテンツ | `words/index.html` | ブログカードの再設計、静的プレビュー文言の追加、更新目安の撤廃 |
| ギャラリー | `photo/index.html`, `painting/index.html`, `js/gallery.js`, `styles/subpage.css` | 横長比率表示、クリックによる原寸表示、ホバー挙動見直し、遅延読み込み維持 |
| アセット | `content/photos.json`, `img/写真/*.jpg` | HEIC参照の解消 (JPEG 変換) |
| デプロイ | Vercel | `vercel --prod` デプロイ実行済み |

## 3. 成果物

1. 「言葉」ページのブログセクションがカード形式で3ブログ（まるいそら／旧まるいそら音楽出版／旧まるいそら音楽出版アーカイブ）を紹介し、各カードにタイトル・説明・静的プレビュー（最終更新日／記事タイトル／抜粋）を表示。
2. ヒーロー文言を2行構成に変更し、「▌」など余計な記号を含まない。
3. 写真・絵画ギャラリーのサムネイルはアスペクト比 4:3 の枠内で `object-fit: contain` を使用し、縦長画像も見切れずに表示。
4. すべてのカードがアンカー化され、クリックで原寸画像（新規タブ）が開く。遅延読み込み (`IntersectionObserver`) は従来通り有効。
5. HEIC 形式の参照を排除し、JSON は JPEG のパスを持つ。対応する `.jpg` ファイルを `img/写真` に配置済み。（`.gitignore` 対象のためリポジトリ外で管理）
6. 写真カードのホバー時に余計なメタ情報が重なって表示されない。

## 4. 機能要件

### 4.1 「言葉」ページ
- ブログカードは以下の情報を固定テキストで表示する。
  - ブログ名
  - 説明文（最多 2 文）
  - 「最終更新」日付（YYYY.MM.DD）
  - 最新記事タイトル（1 行）
  - 抜粋（1〜2 行）
  - 「ブログを開く」リンク (外部タブ, `rel="noopener"`)
- カードは正方形グリッド（最小幅 260px）でレスポンシブ対応。
- 12:00 更新予定表示は不要。
- ヒーロー文言は 2 行で `<br>` を使用。スクリーンリーダー向け特別要素は不要。

### 4.2 ギャラリー共通 (写真／絵画)
- `js/gallery.js`
  - 各カード `<article>` 内に `<a>` を含め、`href` に原寸画像 URL を設定。
  - クリック時は `target="_blank"` `rel="noopener"`。
  - 遅延読み込みは `img[data-src]`＋`IntersectionObserver` を継続。
  - `photo-metadata` などホバー時メタ情報用 DOM を生成しない。
- スタイル (`styles/subpage.css`)
  - `.art-figure` / `.photo-figure` の `aspect-ratio` を 4:3 に設定。
  - `.photo-wrapper` で `object-fit: contain` を適用し、背景色は既存テーマと調和。
  - `.photo-card` のホバー時アニメーションは影・位置のみ。文字重なり無し。
- ホバー時に余計なキャプションは表示しない。

### 4.3 写真アセット
- `content/photos.json` で HEIC 拡張子は使用しない。
- HEIC を JPEG に変換し、同名 `.jpg` を `img/写真` 配下に格納。
- JSON に記載された全パスは `npm run start` 時および Vercel 上で有効であること。

## 5. 非機能要件

- **ブラウザ互換性**: 最新の Chrome, Firefox, Safari, Edge で表示崩れがないこと。
- **アクセシビリティ**: カード全体がアンカーとなるため、キーボード操作でフォーカス可能であること。
- **パフォーマンス**: 遅延読み込みを維持し、初期ロードが極端に遅くならない。
- **デプロイ**: Vercel への `vercel --prod` デプロイを完了し、本番URLで動作確認済み。

## 6. 受け入れ条件

| ID | シナリオ | 測定方法 |
|----|-----------|-----------|
| AC-1 | 「言葉」ページを開くと3つのブログカードが表示され、各カードのリンクが正しく外部ブログに遷移する | 手動確認 |
| AC-2 | ヒーローの説明文が2行で改行され、記号が含まれない | 手動確認 |
| AC-3 | 写真・絵画ギャラリーで縦長画像でも上下が切れずに表示される | 手動確認 (サンプル：F-107, F-140, F-144) |
| AC-4 | ギャラリーの画像をクリックすると原寸画像が新規タブで開く | 手動確認 |
| AC-5 | 写真カードをホバーしても「Flux ***」等の不要テキストが表示されない | 手動確認 |
| AC-6 | `content/photos.json` の各 `src` が 200 応答を返す | `npm run start` + ブラウザ検証 |

## 7. スコープ外

- ブログプレビューの自動取得 (RSS/スクレイピング) は対象外。静的テキストで運用。
- 新規ブログの追加やカードレイアウトの動的変更。
- ギャラリーのモーダルビュー実装。
- 画像圧縮や遅延読み込みロジックの刷新。

## 8. リスクと課題

| リスク/課題 | 影響 | 対応策 |
|-------------|------|--------|
| `.jpg` ファイルが `.gitignore` 対象でリポジトリ共有されない | デプロイ環境によっては404になる | 画像は Vercel プロジェクトに手動アップロード済み。追加時は同手順を徹底 |
| ブログプレビューの情報が古くなる | 利用者へ誤情報を与える可能性 | 運用で定期チェック。更新頻度が低いため影響は限定的 |
| Vercel CLI のタイムアウト警告 | デプロイ確認が遅れる | 実際にはデプロイ成功。ログを必ず確認し、URLで動作検証 |

## 9. 今後の改善余地 (任意)

- ブログプレビューを自動更新する RSS 連携や Note API 連携の検討。
- ギャラリーのモーダル・スライドショー機能追加。
- 画像最適化 (WebP 生成、レスポンシブソース) の導入。
- ギャラリーデータの分類・タグ機能追加。
